<div class="row">
  <div class="col-12">
    <h1 class="center-text">Products list</h1>
  </div>
</div>
<div class="d-flex justify-content-between align-items-start flex-wrap">
  {{#each productsItems}}
  <div class="card mb-5 " style="width: 18rem;">
    <img class="img-responsive mx-auto p-3" srcset={{this.image}} width="200" height="200">
    <div class="card-body">
      <h5 class="card-title">{{ this.name }}</h5>
      <p class="price pull-left fw-bold">{{ this.price }} €</p>
      <p class="card-text">{{ this.description }}</p>
      <div class="d-grid gap-2 d-md-flex justify-content-md-end">
        <a href="/users/add-product/{{this._id}}" class="btn btn-primary pull-rigth">Add to shopping</a>
      </div>
    </div>
  </div>
  {{/each}}
</div>